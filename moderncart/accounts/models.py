# The accounts app relies on Django's built-in User model (django.contrib.auth).
# No custom models are needed — registration, login and the dashboard all
# operate on the standard User model plus the Order/Cart models from `store`.
